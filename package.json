{
  "name": "tafseertube",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && sirv docs --no-clear --dev",
    "start": "sirv docs --no-clear",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "prettier": "^3.0.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "sirv-cli": "^2.0.0"
  }
}
